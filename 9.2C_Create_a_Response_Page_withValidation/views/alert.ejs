<%- include('header'); -%>

    <header>
        <div class="container-fluid bg-primary-subtle">
            <div class="col-sm-8 mx-auto text-center py-2">
                <h1 class="display-4">dKin Caps</h1>
                <p class="lead ">A young and inspiring organisation whose
                    primary purpose is to bring you the highest quality headwear available.</p>
            </div>
        </div>
    </header>

    <main>
        <div class="container">
            <h3 class="mt-3">Errors in form</h3>

            <p class="text-secondary"> 
                Sorry, there seems to be a problem with the data you supplied. Your form has a total of <%= count %> errors. 
              
            </p>

            <h5>Errors List</h5>

            <div class="text-secondary">
                <ol>
                    <% if (!firstname) { %> <!-- If firstname is not filled, show the row -->
                    <li scope="row">Error in field 'firstname': <em>Missing firstname?</em></li>
                    <% } %>
                    <% if (!surename) { %> <!-- If surename is not filled, show the row -->
                    <li scope="row">Error in field 'surename': <em>Missing surename?</em></li>
                    <% } %>

                    <% if (!email) { %> <!-- If surename is not filled, show the row -->
                    <li scope="row">Error in field 'email': <em>Missing or invalid email?</em></li>
                    <% } %>


                    <% if (!mobile) { %> <!-- If surename is not filled, show the row -->
                        <li scope="row">Error in field 'mobile number': <em>Missing or invalid mobile?</em></li>
                    <% } %>


                    <% if (!numcaps) { %> <!-- If surename is not filled, show the row -->
                        <li scope="row">Error in field 'numcaps': <em>Number of caps incorrect?</em></li>
                    <% } %>

                    <% if (!capstyle) { %> <!-- If surename is not filled, show the row -->
                        <li scope="row">Error in field 'capstyle': <em>Capstyle incorrect?</em></li>
                    <% } %>

                    <% if (!comments) { %> <!-- If surename is not filled, show the row -->
                    <li scope="row">Error in field 'comments': <em>Missing comments?</em></li>
                    <% } %>
                </ol> 
                <a href="/" class="">Click here to return to the feedback form</a>
            </div>
        </div>
    </main>

    <%- include('footer'); -%>